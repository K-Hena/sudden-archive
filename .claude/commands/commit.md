# /commit

변경된 파일을 확인하고 커밋까지 진행한다.

## 절차
1. `git status`로 변경사항 확인
2. `git diff`로 실제 변경 내용 확인
3. 기능 추가 / 버그 수정 / 문서 수정 중 하나로 분류해 커밋 메시지 작성
4. `git add` → `git commit` → `git push`
5. Vercel 자동 배포 확인 (push 안 하면 배포 안 됨)

docs/PROMPTS.md "6. Git 작업"과 동일한 절차를 실행 단축키로 만든 것.
